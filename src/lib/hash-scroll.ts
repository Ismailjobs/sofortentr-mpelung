/** Inline hash helper — keep in sync with layout Script id="hash-scroll". */
export const HASH_SCROLL_SCRIPT = `(function(){function norm(h){if(!h)return"";var id=h.replace(/^#/,"").split("#").filter(Boolean)[0]||"";if(id==="start")return"";return id?"#"+id:"";}function syncUrl(h){var want=norm(h),path=location.pathname+location.search;if(!want){if(location.hash)history.replaceState(null,"",path);return"";}if(location.hash!==want)history.replaceState(null,"",path+want);return want;}function scrollTo(h){if(!h)return;var el=document.getElementById(h.slice(1));if(el)requestAnimationFrame(function(){el.scrollIntoView({behavior:"smooth",block:"start"});});}syncUrl(location.hash);window.addEventListener("hashchange",function(){scrollTo(syncUrl(location.hash));},{passive:true});})();`;
