/** Responsive `sizes` für Service-Karten (Startseite + /leistungen Grid). */
export const SERVICE_CARD_IMAGE_SIZES =
  "(max-width: 639px) 360px, (max-width: 1279px) 440px, 320px";

/** Header/Footer-Logo — max. Anzeige ~360×84 CSS px. */
export const SITE_LOGO_SIZES = "(max-width: 639px) 240px, (max-width: 1147px) 320px, 360px";

/** WhyUs-Transporter — zentrale Spalte, max ~880px breit. */
export const TRUCK_IMAGE_SIZES = "(max-width: 640px) 400px, (max-width: 1024px) 720px, 880px";
