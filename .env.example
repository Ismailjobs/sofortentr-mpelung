# Kopieren nach `.env.local` (wird nicht committet). Contact-API lädt diese Dateien aus dem Projektroot.
# Brevo → https://app.brevo.com/settings/keys/api  („API Keys“ / Transactional)

BREVO_API_KEY=
